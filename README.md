# భజనమాల

Simple song search. Everything lives in this folder (app + page images in `public/pages`).

## Live URL

After GitHub Pages is enabled, the site will be:

**https://gayathrimatha.github.io/bhajanamala/**

## Local

```bash
npm install
npm run dev
```

```bash
npm run build
npm run preview
```
